#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n,l,num,cnt;
    deque<pair<int,int>> d;
    cin >> n >> l;
    for(int i=0;i<n;i++){
        cin >> num;
        if(d.size()!=0){

        }
        else d.push_back(num,i);
    }

    return 0;
}

1

 